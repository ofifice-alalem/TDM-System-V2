# 🏗️ ARCHITECTURE.md

## Taqnia Distribution Manager — File Structure Guide (Blade)

---

## 🎯 هدف هذا الملف
هذا الملف لا يشرح **منطق العمليات** ولا **سير الطلبات**.

👉 كل عملية لها ملفها الخاص.

هدف هذا الملف فقط:

> **توحيد طريقة تنظيم الملفات لأي عملية داخل المشروع**

بحيث:
- أي عملية جديدة تُبنى بنفس النسق
- أي مطوّر أو AI agent يعرف أين يضع كل ملف
- لا فوضى ولا تكرار

---

## 🧠 الفلسفة الأساسية

1. كل **عملية = وحدة مستقلة**
2. الفصل دائمًا حسب:
   - الدور (Marketer / Warehouse)
   - ثم نوع العملية (Requests / Sales / Returns / Withdrawals …)
3. لا نخلط ملفات عمليات مختلفة
4. كل ما هو مشترك يوضع في `Shared`

---

## 🧱 التقسيم الرئيسي للمشروع

```
app/
├── Http/
│   └── Controllers/
│       ├── Marketer/
│       ├── Warehouse/
│       └── Shared/
│
├── Domains/
│   ├── Marketer/
│   ├── Warehouse/
│   └── Shared/
│
├── Models/
├── Services/
└── Policies/

resources/
└── views/
    ├── marketer/
    ├── warehouse/
    └── shared/
```

---

## 🧩 النسق المعتمد لأي عملية واحدة

### مثال افتراضي: `عملية X`

> **هذا مجرد قالب تنظيمي** — لا يشرح المنطق

---

### 1️⃣ Controllers

```
Http/Controllers/
├── Marketer/
│   └── X/
│       └── MarketerXController.php
│
├── Warehouse/
│   └── X/
│       └── WarehouseXController.php
│
└── Shared/
    └── X/
        └── XHelperController.php (إن وُجد)
```

🔹 كل Controller مسؤول عن **دور واحد فقط**

---

### 2️⃣ Domain / Service (منطق الأعمال)

```
Domains/
├── Marketer/
│   └── X/
│       └── MarketerXService.php
│
├── Warehouse/
│   └── X/
│       └── WarehouseXService.php
│
└── Shared/
    └── X/
        └── XService.php (مشترك إن لزم)
```

🔹 لا منطق أعمال داخل Controllers

---

### 3️⃣ Models

```
Models/
└── X/
    ├── X.php
    ├── XItem.php
    └── XLog.php (إن وُجد)
```

🔹 الموديلات مرتبطة بالعملية نفسها

---

### 4️⃣ Views (Blade)

```
resources/views/
├── marketer/
│   └── x/
│       ├── index.blade.php
│       ├── create.blade.php
│       └── show.blade.php
│
├── warehouse/
│   └── x/
│       ├── index.blade.php
│       ├── review.blade.php
│       └── document.blade.php
│
└── shared/
    └── x/
        └── partials/
            └── table.blade.php
```

🔹 لا Views مشتركة خارج `shared`

---

### 5️⃣ Routes

```
routes/
├── marketer.php
│   └── Route::prefix('x')->group(...)
│
├── warehouse.php
│   └── Route::prefix('x')->group(...)
```

🔹 نفس العملية
🔹 مسارات مختلفة حسب الدور

---

## ♻️ قاعدة التعميم (إلزامية)

أي عملية جديدة في النظام:

- طلب بضاعة
- بيع
- توثيق
- إرجاع
- سحب أرباح
- سداد ديون

❗ **تستخدم نفس هذا النسق حرفيًا**

بدون:
- دمج Controllers
- أو وضع ملفات في أماكن عامة
- أو اختصار مخالف للهيكل

---

## 🛡️ قواعد صارمة

1. Controller = توجيه فقط
2. Service = منطق فقط
3. View = عرض فقط
4. لا Controller عام لكل شيء
5. Shared فقط لما هو مشترك فعلاً

---

## ✅ الخلاصة

هذا الملف يحدد:

> **أين يُوضع الملف، وليس ماذا يفعل**

وأي خروج عن هذا النسق:
❌ يُعتبر خطأ معماري

